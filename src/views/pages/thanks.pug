extends ../layouts/default
include ../helpers/mixins

block variables
	- const title = (lang === 'ru') ? "Kaleydo - спасибо за заявку" : "Kaleydo - thanks for request"
	- const description = (lang === 'ru') ? "Просканируйте нужный вам цвет и просматрите его через приложение «Kaleydo Evo mini»" : "Capture with Kaleydo EVO mini every color"

block content
	include ../../blocks/modules/thanks/thanks
	+thanks({})
block scripts
	script(src="js/main.js")
	script(src="js/vendor.js")
